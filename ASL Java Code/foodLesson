import javax.swing.*;
import java.awt.*;
import java.awt.event.*;
import java.sql.*;
/**
 * Started by Cameron on April 3rd
 * Edited by Cameron on April 5th and 8th
 * make a timer so button unclickable for amount of time
 * maybe use if statement wih it
 */

public class foodLesson{
    private Connection conn;
    private Statement stmt;
    private ResultSet rs;
    private JFrame frame = new JFrame("Food and Fruits Lesson");
    private JPanel panel = new JPanel();
    private JLabel message = new JLabel("<html>Learn some of food names.<br/>" +
        "Must finish lesson before leaving.</html>");
    private JLabel apple = new JLabel("Apple");
    private JLabel orange = new JLabel("Orange");
    private JLabel banana = new JLabel("Banana");
    private JLabel strawberry = new JLabel("Strawberry");
    private JLabel lemon = new JLabel("Lemon");
    private JLabel cherry = new JLabel("Cherry");
    private JLabel watermelon = new JLabel("Watermelon");
    private JLabel pasta = new JLabel("Pasta");
    private JLabel meat = new JLabel("Meat");
    private JLabel chicken = new JLabel("Chicken");
    private JLabel eggs = new JLabel("Eggs");
    private JButton endButton = new JButton("Done");
    public foodLesson(){
        panel.setLayout(new GridBagLayout());        
        endButton.addActionListener(new ActionListener(){
            public void actionPerformed(ActionEvent event){
                JOptionPane.showMessageDialog(null, "Lesson Completed!");
                frame.dispose();
                new Lesson();
            }
        });
        GridBagConstraints gbc = new GridBagConstraints();
        gbc.gridwidth = GridBagConstraints.REMAINDER;
        gbc.anchor = GridBagConstraints.NORTH;
        
        panel.add(message, gbc);
        
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ foodGifs⁩ ▸ apple.gif")), gbc);
        panel.add(apple, gbc);
        panel.add(new JLabel(new ImageIcon("C:‎⁨Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ orange.gif")), gbc);
        panel.add(orange, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ banana⁩.gif")), gbc);
        panel.add(banana, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ strawberry.gif")), gbc);
        panel.add(strawberry, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ lemon.gif")), gbc);
        panel.add(lemon, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ cherry.gif")), gbc);
        panel.add(cherry, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ watermelon.gif")), gbc);
        panel.add(watermelon, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ pasta⁩.gif")), gbc);
        panel.add(pasta, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ meat.gif")), gbc);
        panel.add(meat, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ ⁩chicken.gif")), gbc);
        panel.add(chicken, gbc);
        panel.add(new JLabel(new ImageIcon("C:Untitled⁩ ▸ ⁨Users⁩ ▸ ⁨omarchaghlil⁩ ▸ ⁨eclipse-workspace⁩ ▸ ⁨ASL⁩ ▸ ⁨src⁩ ▸ ⁨foodGifs ▸ eggs.gif")), gbc);
        panel.add(eggs, gbc);
        //panel.add(questionTwoInput, gbc);
        //more quiz questions in the above order
        //panel.add(password, gbc);
        //panel.add(passInput, gbc);
        panel.add(endButton, gbc);
    
        JScrollPane scrPanel = new JScrollPane(panel);
        //JPanel buttons = new JPanel(new GridBagLayout());
        //buttons.add(endButton, gbc);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setExtendedState(JFrame.MAXIMIZED_BOTH);
        frame.setContentPane(scrPanel);
        frame.pack();
        frame.setVisible(true);
    }
}
